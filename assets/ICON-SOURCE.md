# Icon source

The CC Status icon is the supplied dark CLI panel artwork with a small terminal
prompt and three lower-right status buttons in red, yellow, and green.
`CCStatus.png` is the canonical artwork used for both the tray
icon and desktop shortcuts. `installer/Build-Icon.cjs` rasterizes that same
image into the Windows multi-size ICO.
